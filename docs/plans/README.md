# docs/plans/

Executor-ready implementation plans（一個 feature 一個 `<feature>.md`），由 dev wrapper（`../DOF-knowledge-base-dev/`）嘅 `/park` skill 產生，commit 落 feature branch。

規則 → [../dev-guide.md](../dev-guide.md)。Vault backlog item 管 WHAT/WHY，呢度管 HOW。
